env: {}
image:
  pullPolicy: IfNotPresent
  repository: dlandon/z80pack
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 4200
        protocol: TCP
        targetPort: 4200

env: {}
image:
  pullPolicy: IfNotPresent
  repository: markusmcnugen/sftp
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 22
        protocol: TCP
        targetPort: 22

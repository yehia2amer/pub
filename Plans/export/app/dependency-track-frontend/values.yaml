env:
  API_BASE_URL: http://192.168.50.252:8081
image:
  pullPolicy: IfNotPresent
  repository: dependencytrack/frontend
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

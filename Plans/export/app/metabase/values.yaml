env:
  MB_DB_FILE: /tmp/metabase.db
image:
  pullPolicy: IfNotPresent
  repository: metabase/metabase
  tag: latest
persistence:
  configpath:
    enabled: true
    mountPath: /tmp
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

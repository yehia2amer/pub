env:
  LOCAL_NET: 192.168.1.0/24
  OPENVPN_FILE: Frankfurt.ovpn
image:
  pullPolicy: IfNotPresent
  repository: alturismo/xteve_vpn
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

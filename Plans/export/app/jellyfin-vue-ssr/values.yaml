env: {}
image:
  pullPolicy: IfNotPresent
  repository: jellyfin/jellyfin-vue
  tag: unstable-ssr
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

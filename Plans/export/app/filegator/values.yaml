env: {}
image:
  pullPolicy: IfNotPresent
  repository: maxime1907/filegator
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

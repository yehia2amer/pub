env:
  MySQL Password: owncloud
  PHP Version: '7.2'
image:
  pullPolicy: IfNotPresent
  repository: dlandon/owncloud
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

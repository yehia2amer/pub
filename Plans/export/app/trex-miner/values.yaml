env:
  ALGO: ethash
  API_PASSWORD: ''
  SERVER: ''
  WALLET: ''
  WORKER: ''
image:
  pullPolicy: IfNotPresent
  repository: ptrfrll/nv-docker-trex
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 4067
        protocol: TCP
        targetPort: 4067

env: {}
image:
  pullPolicy: IfNotPresent
  repository: mojo42/jirafeau
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
  storage(config):
    enabled: true
    mountPath: /www/lib/config.local.php
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

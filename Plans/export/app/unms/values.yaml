env: {}
image:
  pullPolicy: IfNotPresent
  repository: nico640/docker-unms
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  Admin Password: changeme2019
  Splunk start args: --accept-license --no-prompt
image:
  pullPolicy: IfNotPresent
  repository: atunnecliffe/splunk
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

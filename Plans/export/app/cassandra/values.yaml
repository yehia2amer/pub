env: {}
image:
  pullPolicy: IfNotPresent
  repository: cassandra
  tag: latest
persistence:
  databasestoragelocation:
    enabled: true
    mountPath: /var/lib/cassandra
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

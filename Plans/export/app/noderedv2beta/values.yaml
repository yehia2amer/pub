env:
  NODE_RED_ENABLE_SAFE_MODE: 'false'
image:
  pullPolicy: IfNotPresent
  repository: nodered/node-red-dev
  tag: beta
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

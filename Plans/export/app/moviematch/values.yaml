env:
  PLEX_TOKEN: ''
  PLEX_URL: ''
image:
  pullPolicy: IfNotPresent
  repository: lukechannings/moviematch
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 8000
        protocol: TCP
        targetPort: 8000

env: {}
image:
  pullPolicy: IfNotPresent
  repository: knex666/mopidy
  tag: 3-experimental
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

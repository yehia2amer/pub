env:
  ADD: shiny
  PASSWORD: yourpasswordhere
  ROOT: 'FALSE'
image:
  pullPolicy: IfNotPresent
  repository: rocker/rstudio
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

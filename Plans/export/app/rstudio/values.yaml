env:
  Add Shiny Server: shiny
  Password: yourpasswordhere
  Root: 'FALSE'
image:
  pullPolicy: IfNotPresent
  repository: rocker/rstudio
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

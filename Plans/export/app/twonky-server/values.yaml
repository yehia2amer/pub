env: {}
image:
  pullPolicy: IfNotPresent
  repository: dtjs48jkt/twonkyserver
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  data:
    enabled: true
    mountPath: /data
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

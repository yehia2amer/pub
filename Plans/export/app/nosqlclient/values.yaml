env:
  MONGO_URL: ''
image:
  pullPolicy: IfNotPresent
  repository: mongoclient/mongoclient
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /data/db
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  ROOT_URL: /
image:
  pullPolicy: IfNotPresent
  repository: qmcgaw/stackedit
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8000'
        protocol: TCP
        targetPort: '8000'

env: {}
image:
  pullPolicy: IfNotPresent
  repository: chvb/docker-dvblink
  tag: latest
persistence:
  dvblink:
    enabled: true
    mountPath: /opt/DVBLink
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

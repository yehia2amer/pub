env:
  DB_SQLITE_FILE: /data/database.sqlite
image:
  pullPolicy: IfNotPresent
  repository: jc21/nginx-proxy-manager
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

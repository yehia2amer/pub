env:
  Memcache Password: ''
  Memcached Username: ''
image:
  pullPolicy: IfNotPresent
  repository: bitnami/memcached
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

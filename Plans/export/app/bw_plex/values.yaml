env:
  token: ''
  url: ''
image:
  pullPolicy: IfNotPresent
  repository: hellowlol/bw_plex
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  mediapath:
    enabled: true
    mountPath: /mnt/user/Media/
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

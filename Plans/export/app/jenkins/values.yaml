env: {}
image:
  pullPolicy: IfNotPresent
  repository: jenkins/jenkins
  tag: latest
persistence:
  jenkins_home:
    enabled: true
    mountPath: /var/jenkins_home
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

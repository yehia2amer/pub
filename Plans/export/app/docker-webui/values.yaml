env:
  CIRCLE: 'yes'
  HOST: ''
  TITLE: Docker WebUI
  UNRAID_IP: ''
image:
  pullPolicy: IfNotPresent
  repository: olprog/unraid-docker-webui
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
    readOnly: true
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 1111
        protocol: TCP
        targetPort: 8080

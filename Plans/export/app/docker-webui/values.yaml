env:
  CIRCLE: 'yes'
  HOST: ''
  TITLE: Docker WebUI
  UNRAID_IP: ''
image:
  pullPolicy: IfNotPresent
  repository: olprog/unraid-docker-webui
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  SECRET_KEY_BASE: secret
image:
  pullPolicy: IfNotPresent
  repository: openproject/community
  tag: '12'
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  RUST_LOG: debug
image:
  pullPolicy: IfNotPresent
  repository: orhunp/rustypaste
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

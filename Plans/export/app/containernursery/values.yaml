env:
  CN_LOG_JSON: 'false'
  CN_LOG_LEVEL: info
image:
  pullPolicy: IfNotPresent
  repository: ghcr.io/itsecholot/containernursery
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

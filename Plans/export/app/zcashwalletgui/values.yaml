env:
  VNC only: 'false'
  VNC password: vncpassword
  VNC resolution: 1280x1024
image:
  pullPolicy: IfNotPresent
  repository: ulisses1478/zcashwallet
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

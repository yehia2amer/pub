env: {}
image:
  pullPolicy: IfNotPresent
  repository: dependencytrack/apiserver
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 8081
        protocol: TCP
        targetPort: 8080

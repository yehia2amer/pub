env: {}
image:
  pullPolicy: IfNotPresent
  repository: yacy/yacy_search_server
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /opt/yacy_search_server/DATA
service:
  http:
    enabled: true
    ports:
      http:
        enabled: true
        port: 8090
        protocol: TCP
        targetPort: 8090
  https:
    enabled: true
    ports:
      https:
        enabled: true
        port: 8443
        protocol: TCP
        targetPort: 8443

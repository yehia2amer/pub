env: {}
image:
  pullPolicy: IfNotPresent
  repository: ghcr.io/dusk-labs/dim
  tag: dev
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

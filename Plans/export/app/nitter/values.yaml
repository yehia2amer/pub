env: {}
image:
  pullPolicy: IfNotPresent
  repository: zedeus/nitter
  tag: latest
persistence:
  nitter-conf:
    enabled: true
    mountPath: /src/nitter.conf
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

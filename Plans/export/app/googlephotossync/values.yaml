env: {}
image:
  pullPolicy: IfNotPresent
  repository: rix1337/docker-gphotos-sync
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  hostpath1:
    enabled: true
    mountPath: /storage
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: rix1337/docker-gphotos-sync
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  hostpath1:
    enabled: true
    mountPath: /storage
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

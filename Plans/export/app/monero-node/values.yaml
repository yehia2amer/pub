env:
  Daemon (user): monerod
image:
  pullPolicy: IfNotPresent
  repository: kannix/monero-full-node
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  -log-level: '2'
image:
  pullPolicy: IfNotPresent
  repository: signtools/signtools
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

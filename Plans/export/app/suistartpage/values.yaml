env: {}
image:
  pullPolicy: IfNotPresent
  repository: corneliousjd/sui-startpage
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /usr/share/nginx/html
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8489'
        protocol: TCP
        targetPort: '80'

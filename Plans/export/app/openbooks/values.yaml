env: {}
image:
  pullPolicy: IfNotPresent
  repository: evanbuss/openbooks
  tag: latest
persistence:
  storage(books):
    enabled: true
    mountPath: /books
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

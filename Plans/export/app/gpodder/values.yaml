env:
  PASSWORD: ''
image:
  pullPolicy: IfNotPresent
  repository: xthursdayx/gpodder-docker
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  downloadspath:
    enabled: true
    mountPath: /downloads
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

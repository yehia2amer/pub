env: {}
image:
  pullPolicy: IfNotPresent
  repository: paulpoco/node
  tag: v3.4.0-alpha2
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

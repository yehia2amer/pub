env: {}
image:
  pullPolicy: IfNotPresent
  repository: maildev/maildev
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 1080
        protocol: TCP
        targetPort: 80
  port(smtp):
    enabled: true
    ports:
      port(smtp):
        enabled: true
        port: 1025
        protocol: TCP
        targetPort: 25

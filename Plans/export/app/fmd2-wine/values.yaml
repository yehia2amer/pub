env:
  UMASK: '002'
image:
  pullPolicy: IfNotPresent
  repository: banhcanh/docker-fmd2
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  CUSTOM_RES_H: '768'
  CUSTOM_RES_W: '1024'
  DATA_PERM: '770'
  UMASK: '000'
image:
  pullPolicy: IfNotPresent
  repository: ich777/enpass
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

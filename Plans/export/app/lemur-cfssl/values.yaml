env: {}
image:
  pullPolicy: IfNotPresent
  repository: halianelf/lemur
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

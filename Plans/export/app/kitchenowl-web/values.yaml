env:
  BACK_URL: http://localhost:5000
image:
  pullPolicy: IfNotPresent
  repository: tombursch/kitchenowl-web
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  BACK_URL: http://localhost:5000
image:
  pullPolicy: IfNotPresent
  repository: tombursch/kitchenowl-web
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8123'
        protocol: TCP
        targetPort: '80'

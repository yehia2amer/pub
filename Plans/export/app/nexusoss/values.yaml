env: {}
image:
  pullPolicy: IfNotPresent
  repository: sonatype/nexus3
  tag: latest
persistence:
  nexusdata:
    enabled: true
    mountPath: /nexus-data
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

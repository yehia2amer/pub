env: {}
image:
  pullPolicy: IfNotPresent
  repository: ckulka/baikal
  tag: nginx
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

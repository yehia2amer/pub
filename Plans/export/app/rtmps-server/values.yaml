env:
  SSL_DOMAIN: rtmp-server.loc
image:
  pullPolicy: IfNotPresent
  repository: jamiephonic/rtmps-hls-server
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

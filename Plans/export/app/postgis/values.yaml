env:
  POSTGRES_PASSWORD: ''
image:
  pullPolicy: IfNotPresent
  repository: postgis/postgis
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /var/lib/postgresql/data
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

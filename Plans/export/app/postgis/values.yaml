env:
  Password for postgres role: ''
image:
  pullPolicy: IfNotPresent
  repository: postgis/postgis
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

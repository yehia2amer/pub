env: {}
image:
  pullPolicy: IfNotPresent
  repository: cncjs/cncjs
  tag: v1.9.22
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

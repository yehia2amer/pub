env: {}
image:
  pullPolicy: IfNotPresent
  repository: tynor88/socat
  tag: latest
persistence: {}
service:
  forwardingport:
    enabled: true
    ports:
      forwardingport:
        enabled: true
        port: PGID
        protocol: TCP
        targetPort: PGID
  listeningport:
    enabled: true
    ports:
      listeningport:
        enabled: true
        port: '443'
        protocol: TCP
        targetPort: '443'

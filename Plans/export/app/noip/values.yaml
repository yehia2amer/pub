env: {}
image:
  pullPolicy: IfNotPresent
  repository: coppit/no-ip
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

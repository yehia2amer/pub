env:
  PHP_TZ: America/New_York
image:
  pullPolicy: IfNotPresent
  repository: rclone/rclone
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: jbreed/nessus
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8834'
        protocol: TCP
        targetPort: '8834'

env: {}
image:
  pullPolicy: IfNotPresent
  repository: antelle/keeweb
  tag: latest
persistence:
  storage(nginx):
    enabled: true
    mountPath: /etc/nginx/external/
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8003'
        protocol: TCP
        targetPort: '443'

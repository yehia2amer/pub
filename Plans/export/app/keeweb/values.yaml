env: {}
image:
  pullPolicy: IfNotPresent
  repository: antelle/keeweb
  tag: latest
persistence:
  storage(nginx):
    enabled: true
    mountPath: /etc/nginx/external/
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

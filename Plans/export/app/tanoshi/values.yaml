env: {}
image:
  pullPolicy: IfNotPresent
  repository: faldez/tanoshi
  tag: latest
persistence:
  directory(config):
    enabled: true
    mountPath: /tanoshi
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: faldez/tanoshi
  tag: latest
persistence:
  directory(config):
    enabled: true
    mountPath: /tanoshi
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 80
        protocol: TCP
        targetPort: 80

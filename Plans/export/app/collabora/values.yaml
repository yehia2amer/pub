env:
  domain: cloud\\.yourdomain\\.com
  password: yourpassword
  username: admin
image:
  pullPolicy: IfNotPresent
  repository: collabora/code
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 9980
        protocol: TCP
        targetPort: 9980

env:
  Domain: cloud\\.yourdomain\\.com
  Password: yourpassword
  Username: admin
image:
  pullPolicy: IfNotPresent
  repository: collabora/code
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

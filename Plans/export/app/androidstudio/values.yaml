env:
  UMASK: '000'
image:
  pullPolicy: IfNotPresent
  repository: jassycliq/android-studio-projector
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

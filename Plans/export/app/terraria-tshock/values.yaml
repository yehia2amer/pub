env:
  GAME_PARAMS: -config serverconfig.txt
image:
  pullPolicy: IfNotPresent
  repository: ich777/terrariaserver
  tag: tshock
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: portainerci/portainer
  tag: '2.1'
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

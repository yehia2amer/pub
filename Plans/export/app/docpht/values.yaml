env:
  '443': '8443'
image:
  pullPolicy: IfNotPresent
  repository: docpht/docpht
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  DOCS_BASE_URL: ''
image:
  pullPolicy: IfNotPresent
  repository: sismics/docs
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

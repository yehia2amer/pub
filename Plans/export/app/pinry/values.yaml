env: {}
image:
  pullPolicy: IfNotPresent
  repository: getpinry/pinry
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 8438
        protocol: TCP
        targetPort: 80

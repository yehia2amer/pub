env: {}
image:
  pullPolicy: IfNotPresent
  repository: hkalexling/mango
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /root/.config/mango
  data:
    enabled: true
    mountPath: /root/mango
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

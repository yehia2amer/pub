env: {}
image:
  pullPolicy: IfNotPresent
  repository: portainer/portainer-ce
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
  dockersocket:
    enabled: true
    mountPath: /var/run/docker.sock
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

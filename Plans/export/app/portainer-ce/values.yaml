env: {}
image:
  pullPolicy: IfNotPresent
  repository: portainer/portainer-ce
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
  dockersocket:
    enabled: true
    mountPath: /var/run/docker.sock
service:
  sshtunnel:
    enabled: true
    ports:
      sshtunnel:
        enabled: true
        port: 8000
        protocol: TCP
        targetPort: 8000
  webui:
    enabled: true
    ports:
      webui:
        enabled: true
        port: 9996
        protocol: TCP
        targetPort: 9000

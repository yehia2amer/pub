env: {}
image:
  pullPolicy: IfNotPresent
  repository: ghcr.io/siwatinc/humhub
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '85'
        protocol: TCP
        targetPort: '80'

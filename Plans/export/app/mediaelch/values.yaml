env:
  UMASK: '000'
image:
  pullPolicy: IfNotPresent
  repository: masonxx/mediaelch
  tag: latest
persistence:
  mediaelch:
    enabled: true
    mountPath: /shows
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

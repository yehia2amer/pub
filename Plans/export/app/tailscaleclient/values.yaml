env:
  Authentication Key: tskey-xxxxxxxxxxxxxxxxxxxxxxxxx
  Exposed Routes: 192.168.88.0/24
  Hostname / Machine Name: tailscaleunraid
image:
  pullPolicy: IfNotPresent
  repository: hussainalhaddad/tailscale
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

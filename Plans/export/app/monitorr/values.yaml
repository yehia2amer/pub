env: {}
image:
  pullPolicy: IfNotPresent
  repository: monitorr/monitorr
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /app
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

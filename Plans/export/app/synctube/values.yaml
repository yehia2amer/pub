env: {}
image:
  pullPolicy: IfNotPresent
  repository: littlestache/synctube
  tag: latest
persistence:
  storage(config):
    enabled: true
    mountPath: /usr/src/app/user
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  APP_HOST: IP OR HOSTNAME
  DATASOURCE: MM_USERNAME:MM_PASSWORD@tcp(DB_HOST:DB_PORT_NUMBER)/MM_DBNAME?charset=utf8mb4,utf8&readTimeout=30s&writeTimeout=30s
  MM_SQLSETTINGS_DRIVERNAME: mysql
  Streaming Port: '8075'
image:
  pullPolicy: IfNotPresent
  repository: mattermost/mattermost-team-edition
  tag: master
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

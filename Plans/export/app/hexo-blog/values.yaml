env:
  HEXO_PLUGINS: ''
image:
  pullPolicy: IfNotPresent
  repository: fletchto99/hexo-dev-blog
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8080'
        protocol: TCP
        targetPort: '8080'

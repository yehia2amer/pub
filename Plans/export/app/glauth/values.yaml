env: {}
image:
  pullPolicy: IfNotPresent
  repository: glauth/glauth
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /app/config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

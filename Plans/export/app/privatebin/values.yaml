env: {}
image:
  pullPolicy: IfNotPresent
  repository: privatebin/nginx-fpm-alpine
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /srv/data
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

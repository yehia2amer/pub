env: {}
image:
  pullPolicy: IfNotPresent
  repository: privatebin/nginx-fpm-alpine
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /srv/data
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

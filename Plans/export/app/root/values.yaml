env: {}
image:
  pullPolicy: IfNotPresent
  repository: bgameiro/arch-cern-root
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /appdata
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

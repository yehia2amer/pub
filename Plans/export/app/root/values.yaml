env: {}
image:
  pullPolicy: IfNotPresent
  repository: bgameiro/arch-cern-root
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /appdata
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 9999
        protocol: TCP
        targetPort: 8888

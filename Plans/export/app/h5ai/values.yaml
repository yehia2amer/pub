env: {}
image:
  pullPolicy: IfNotPresent
  repository: awesometic/h5ai
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  directory(storage):
    enabled: true
    mountPath: /h5ai
    readOnly: true
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 80
        protocol: TCP
        targetPort: 80

env: {}
image:
  pullPolicy: IfNotPresent
  repository: convos/convos
  tag: stable
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 3520
        protocol: TCP
        targetPort: 3000

env: {}
image:
  pullPolicy: IfNotPresent
  repository: bashninja/demonsaw-client
  tag: latest
persistence:
  demonsaw-client:
    enabled: true
    mountPath: /nobody/downloads
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

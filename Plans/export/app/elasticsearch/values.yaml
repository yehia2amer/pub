env:
  Discovery Type: single-node
image:
  pullPolicy: IfNotPresent
  repository: d8sychain/elasticsearch
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

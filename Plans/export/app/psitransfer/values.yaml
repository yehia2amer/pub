env:
  PSITRANSFER_ADMIN_PASS: secret
image:
  pullPolicy: IfNotPresent
  repository: psitrax/psitransfer
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

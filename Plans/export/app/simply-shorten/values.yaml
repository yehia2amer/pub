env:
  password: unraid
  username: unraid
image:
  pullPolicy: IfNotPresent
  repository: draganczukp/simply-shorten
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

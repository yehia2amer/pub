env:
  Base URL: ''
image:
  pullPolicy: IfNotPresent
  repository: dgtlmoon/changedetection.io
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

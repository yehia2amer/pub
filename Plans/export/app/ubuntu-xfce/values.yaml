env:
  VNC Password: mypassword
image:
  pullPolicy: IfNotPresent
  repository: knilix/ubuntu-xfce-vnc-novnc
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

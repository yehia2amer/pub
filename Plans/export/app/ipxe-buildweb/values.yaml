env: {}
image:
  pullPolicy: IfNotPresent
  repository: angelics/unraid-docker-ipxe-buildweb
  tag: latest
persistence:
  cache:
    enabled: true
    mountPath: /cache
  ipxe:
    enabled: true
    mountPath: /ipxe
  ipxe-buildweb:
    enabled: true
    mountPath: /ipxe-buildweb
  logs:
    enabled: true
    mountPath: /logs
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 80
        protocol: TCP
        targetPort: 80

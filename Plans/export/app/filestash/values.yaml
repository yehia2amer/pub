env: {}
image:
  pullPolicy: IfNotPresent
  repository: machines/filestash
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /app/data/state/
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  UMASK: ''
image:
  pullPolicy: IfNotPresent
  repository: lscr.io/linuxserver/cops
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  pathbooks:
    enabled: true
    mountPath: /books
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

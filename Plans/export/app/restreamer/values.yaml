env:
  RS_PASSWORD: Password
  RS_USERNAME: admin
image:
  pullPolicy: IfNotPresent
  repository: datarhei/restreamer
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

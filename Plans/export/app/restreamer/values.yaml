env:
  RS_PASSWORD: Password
  RS_USERNAME: admin
image:
  pullPolicy: IfNotPresent
  repository: datarhei/restreamer
  tag: latest
persistence:
  datapath:
    enabled: true
    mountPath: /restreamer/db
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

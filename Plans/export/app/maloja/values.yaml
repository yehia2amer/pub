env:
  MALOJA_DATA_DIRECTORY: /config
  MALOJA_HOST: 0.0.0.0
image:
  pullPolicy: IfNotPresent
  repository: krateng/maloja
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

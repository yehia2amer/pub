env:
  Container Host: 0.0.0.0
  Data Directory: /config
image:
  pullPolicy: IfNotPresent
  repository: krateng/maloja
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

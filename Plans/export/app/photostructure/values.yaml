env:
  PS_LOG_LEVEL: ''
image:
  pullPolicy: IfNotPresent
  repository: photostructure/server
  tag: stable
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  ALLOW_EMPTY_PASSWORD: 'yes'
  Password: '941378'
image:
  pullPolicy: IfNotPresent
  repository: bitnami/redis
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

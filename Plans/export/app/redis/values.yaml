env:
  ALLOW_EMPTY_PASSWORD: 'yes'
  REDIS_PASSWORD: '941378'
image:
  pullPolicy: IfNotPresent
  repository: bitnami/redis
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 6379
        protocol: TCP
        targetPort: 6379

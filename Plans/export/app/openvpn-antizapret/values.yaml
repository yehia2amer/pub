env:
  DEFAULT_GATEWAY: 'false'
  DNS: 192.168.104.1
  Firewall: 'true'
image:
  pullPolicy: IfNotPresent
  repository: dperson/openvpn-client
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

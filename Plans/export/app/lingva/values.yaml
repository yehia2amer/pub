env:
  site_domain: ''
image:
  pullPolicy: IfNotPresent
  repository: thedaviddelta/lingva-translate
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '3000'
        protocol: TCP
        targetPort: '3000'

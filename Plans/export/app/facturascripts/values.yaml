env: {}
image:
  pullPolicy: IfNotPresent
  repository: patrickstigler/facturascripts
  tag: latest
persistence:
  configpath:
    enabled: true
    mountPath: /var/www/html
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

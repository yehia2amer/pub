env:
  GAME_PARAMS: -host 16 -exec server.cfg
image:
  pullPolicy: IfNotPresent
  repository: ich777/zandronum
  tag: latest
persistence:
  serverfiles:
    enabled: true
    mountPath: /zandronum
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

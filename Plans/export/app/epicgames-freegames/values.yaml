env: {}
image:
  pullPolicy: IfNotPresent
  repository: charlocharlie/epicgames-freegames
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /usr/app/config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '3055'
        protocol: TCP
        targetPort: '3000'

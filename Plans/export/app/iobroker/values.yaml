env:
  USB Devices: ''
image:
  pullPolicy: IfNotPresent
  repository: buanet/iobroker
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: cloudflare/cloudflared
  tag: 2021.8.2
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: captinsano/tonido
  tag: latest
persistence:
  tonido:
    enabled: true
    mountPath: /unRAID
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 10001
        protocol: TCP
        targetPort: 10001

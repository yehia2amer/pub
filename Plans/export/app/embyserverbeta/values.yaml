env:
  GIDLIST: '2'
image:
  pullPolicy: IfNotPresent
  repository: emby/embyserver
  tag: beta
persistence:
  embyserverbeta:
    enabled: true
    mountPath: /mnt
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

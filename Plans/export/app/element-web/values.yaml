env: {}
image:
  pullPolicy: IfNotPresent
  repository: vectorim/element-web
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /app/config.json
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

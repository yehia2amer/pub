env: {}
image:
  pullPolicy: IfNotPresent
  repository: tloncorp/urbit
  tag: v1.8
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  Application HTTPS Only: ''
  Container Port: ''
image:
  pullPolicy: IfNotPresent
  repository: benbusby/whoogle-search
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

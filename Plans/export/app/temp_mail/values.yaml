env:
  EMAIL: olprog@1secmail.org
image:
  pullPolicy: IfNotPresent
  repository: olprog/tmpmail
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  GUID: '100'
  UMASK: '000'
  USER_ID: '0'
image:
  pullPolicy: IfNotPresent
  repository: coolasice1999/tmm-cli-cronjob
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  tcp-port-0:
    enabled: true
    ports:
      tcp-port-0:
        enabled: true
        port: '5800'
        protocol: TCP
        targetPort: '5800'
  tcp-port-1:
    enabled: true
    ports:
      tcp-port-1:
        enabled: true
        port: '5801'
        protocol: TCP
        targetPort: '5801'

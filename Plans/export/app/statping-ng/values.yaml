env: {}
image:
  pullPolicy: IfNotPresent
  repository: adamboutcher/statping-ng
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /app
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 8366
        protocol: TCP
        targetPort: 8080

env: {}
image:
  pullPolicy: IfNotPresent
  repository: biggiesize/genea-app
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

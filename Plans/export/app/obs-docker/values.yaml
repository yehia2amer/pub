env:
  VNC_PASSWD: ''
image:
  pullPolicy: IfNotPresent
  repository: cieg/obs-docker
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  VNC_PASSWD: ''
image:
  pullPolicy: IfNotPresent
  repository: cieg/obs-docker
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 5901
        protocol: TCP
        targetPort: 5901

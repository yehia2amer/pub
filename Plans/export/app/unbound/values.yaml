env: {}
image:
  pullPolicy: IfNotPresent
  repository: kutzilla/unbound
  tag: latest
persistence:
  configpath:
    enabled: true
    mountPath: /opt/unbound/etc/unbound/
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

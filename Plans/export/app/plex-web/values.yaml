env: {}
image:
  pullPolicy: IfNotPresent
  repository: tronyx/plex-web
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '5000'
        protocol: TCP
        targetPort: '5000'

env:
  DEVICE: ''
  PASS: ''
  USER: ''
image:
  pullPolicy: IfNotPresent
  repository: rix1337/docker-myjd-api
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 8080
        protocol: TCP
        targetPort: 8080

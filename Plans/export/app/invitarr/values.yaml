env:
  token: ''
image:
  pullPolicy: IfNotPresent
  repository: piratify/invitarr
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /app/app/config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

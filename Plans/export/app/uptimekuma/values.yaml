env: {}
image:
  pullPolicy: IfNotPresent
  repository: louislam/uptime-kuma
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /app/data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '3001'
        protocol: TCP
        targetPort: '3001'

env: {}
image:
  pullPolicy: IfNotPresent
  repository: schollz/cowyo
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8050'
        protocol: TCP
        targetPort: '8050'

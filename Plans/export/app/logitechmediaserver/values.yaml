env: {}
image:
  pullPolicy: IfNotPresent
  repository: dlandon/logitechmediaserver
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  musicpath:
    enabled: true
    mountPath: /music
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

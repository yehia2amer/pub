env:
  SQLITE_DATABASE: db_name.db
image:
  pullPolicy: IfNotPresent
  repository: coleifer/sqlite-web
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

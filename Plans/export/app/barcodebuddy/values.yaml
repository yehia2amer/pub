env: {}
image:
  pullPolicy: IfNotPresent
  repository: f0rc3/barcodebuddy-docker
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  API Password: ''
  Wallet Password: ''
image:
  pullPolicy: IfNotPresent
  repository: ghcr.io/siafoundation/siad
  tag: master
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

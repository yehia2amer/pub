env:
  SIA_API_PASSWORD: ''
  SIA_WALLET_PASSWORD: ''
image:
  pullPolicy: IfNotPresent
  repository: ghcr.io/siafoundation/siad
  tag: master
persistence:
  datadirectory:
    enabled: true
    mountPath: /sia-data
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

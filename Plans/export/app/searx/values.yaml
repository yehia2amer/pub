env: {}
image:
  pullPolicy: IfNotPresent
  repository: searx/searx
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /etc/searx
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

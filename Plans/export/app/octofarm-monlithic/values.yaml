env: {}
image:
  pullPolicy: IfNotPresent
  repository: octofarm/octofarm
  tag: monolithic-latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  DOCKER_TLS_CERTDIR: /certs
image:
  pullPolicy: IfNotPresent
  repository: docker
  tag: dind
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

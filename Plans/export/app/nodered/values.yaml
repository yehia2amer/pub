env:
  NODE_RED_ENABLE_SAFE_MODE: 'false'
image:
  pullPolicy: IfNotPresent
  repository: nodered/node-red
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  INSTANCE_NAME: ''
image:
  pullPolicy: IfNotPresent
  repository: searxng/searxng
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /etc/searx
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

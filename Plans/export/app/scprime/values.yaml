env:
  Wallet Password: ''
image:
  pullPolicy: IfNotPresent
  repository: scprime/scprime
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

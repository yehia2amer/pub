env:
  SALT_LOGLEVEL: warning
  SALT_PASSWORD: salty
image:
  pullPolicy: IfNotPresent
  repository: lubeda/salt4unraid
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

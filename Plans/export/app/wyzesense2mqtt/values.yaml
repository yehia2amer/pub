env: {}
image:
  pullPolicy: IfNotPresent
  repository: raetha/wyzesense2mqtt
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

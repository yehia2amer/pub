env: {}
image:
  pullPolicy: IfNotPresent
  repository: monitorr/logarr-alpha
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /app
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

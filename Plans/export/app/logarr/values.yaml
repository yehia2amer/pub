env: {}
image:
  pullPolicy: IfNotPresent
  repository: monitorr/logarr-alpha
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /app
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '80'
        protocol: TCP
        targetPort: '80'

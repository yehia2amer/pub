env:
  UMASK: '000'
  VNC_PASSWORD: ''
  WEBPAGE_TITLE: Tower
image:
  pullPolicy: IfNotPresent
  repository: binhex/arch-pycharm
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

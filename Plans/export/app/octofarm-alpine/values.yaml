env:
  MONGO: mongodb://mongo/octofarm
image:
  pullPolicy: IfNotPresent
  repository: octofarm/octofarm
  tag: alpine-latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: hussainalhaddad/pcmanfm
  tag: latest
persistence:
  configfolder:
    enabled: true
    mountPath: /home/user
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '10000'
        protocol: TCP
        targetPort: '10000'

env: {}
image:
  pullPolicy: IfNotPresent
  repository: raymondmm/tasmoadmin
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 9541
        protocol: TCP
        targetPort: 80

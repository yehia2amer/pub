env:
  TIMECRON: '300'
image:
  pullPolicy: IfNotPresent
  repository: patrickstigler/docker-fetchmail
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /data/etc/
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

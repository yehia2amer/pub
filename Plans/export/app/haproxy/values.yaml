env: {}
image:
  pullPolicy: IfNotPresent
  repository: haproxy
  tag: latest
persistence:
  configurationdirectory:
    enabled: true
    mountPath: /usr/local/etc/haproxy
    readOnly: true
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

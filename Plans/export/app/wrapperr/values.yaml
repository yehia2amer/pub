env: {}
image:
  pullPolicy: IfNotPresent
  repository: aunefyren/wrapperr
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /var/www/html/config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '580'
        protocol: TCP
        targetPort: '80'

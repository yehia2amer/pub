env:
  Admin Interface Port: mypass
image:
  pullPolicy: IfNotPresent
  repository: hexparrot/mineos
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  ID: ''
  PASSWORD: ''
image:
  pullPolicy: IfNotPresent
  repository: ramdockerapp/nut-server
  tag: latest
persistence:
  gamespath:
    enabled: true
    mountPath: /games
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 9000
        protocol: TCP
        targetPort: 9000

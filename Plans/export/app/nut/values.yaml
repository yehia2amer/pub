env:
  ID: ''
  PASSWORD: ''
image:
  pullPolicy: IfNotPresent
  repository: ramdockerapp/nut-server
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

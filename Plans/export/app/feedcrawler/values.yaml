env:
  DEVICE: ''
  PASS: ''
  USER: ''
image:
  pullPolicy: IfNotPresent
  repository: rix1337/docker-feedcrawler
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '9090'
        protocol: TCP
        targetPort: '9090'

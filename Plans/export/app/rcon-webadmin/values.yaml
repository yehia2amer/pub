env:
  RWA_ADMIN: 'TRUE'
  RWA_ENV: 'False'
  RWA_PASSWORD: admin
  RWA_USERNAME: admin
image:
  pullPolicy: IfNotPresent
  repository: itzg/rcon
  tag: latest
persistence:
  db:
    enabled: true
    mountPath: /opt/rcon-web-admin/db
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  PAGE_TITLE: KID
  RECON_DELAY: '20'
  ROOT_PASSWORD: admin
  SCR_HEIGHT: '900'
  SCR_WIDTH: '1600'
  VNC_RESIZE: scale
  noVNC PATH_PREFIX: /
image:
  pullPolicy: IfNotPresent
  repository: msjpq/firefox-vnc
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  ADMINER_DESIGN: ''
  ADMINER_PLUGINS: ''
image:
  pullPolicy: IfNotPresent
  repository: adminer
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 8080
        protocol: TCP
        targetPort: 8080

env: {}
image:
  pullPolicy: IfNotPresent
  repository: cloudrac3r/bibliogram
  tag: latest
persistence:
  config-js:
    enabled: true
    mountPath: /app/config.js
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 10407
        protocol: TCP
        targetPort: 10407

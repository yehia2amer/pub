env:
  SECRET: password
  USER: guest
image:
  pullPolicy: IfNotPresent
  repository: hikariai/nvim-server
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  UMASK: '000'
image:
  pullPolicy: IfNotPresent
  repository: binhex/arch-moviegrabber
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

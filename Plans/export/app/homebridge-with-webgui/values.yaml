env:
  aptpackages: ''
  packages: ''
image:
  pullPolicy: IfNotPresent
  repository: ghcr.io/siwatinc/homebridge_gui
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

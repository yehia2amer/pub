env: {}
image:
  pullPolicy: IfNotPresent
  repository: silverwind/droppy
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  files:
    enabled: true
    mountPath: /files
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env: {}
image:
  pullPolicy: IfNotPresent
  repository: registry
  tag: '2'
persistence:
  datastoragepath:
    enabled: true
    mountPath: /var/lib/registry
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '5000'
        protocol: TCP
        targetPort: '5000'

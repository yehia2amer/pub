env: {}
image:
  pullPolicy: IfNotPresent
  repository: registry
  tag: '2'
persistence:
  datastoragepath:
    enabled: true
    mountPath: /var/lib/registry
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

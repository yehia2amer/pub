env:
  HOME - Dont Change!: /root
image:
  pullPolicy: IfNotPresent
  repository: coderaiser/cloudcmd
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  VIKUNJA_API_URL: https://domain.com/api/v1
image:
  pullPolicy: IfNotPresent
  repository: vikunja/frontend
  tag: latest
persistence: {}
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8079'
        protocol: TCP
        targetPort: '80'

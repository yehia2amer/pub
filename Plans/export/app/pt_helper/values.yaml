env: {}
image:
  pullPolicy: IfNotPresent
  repository: crazyq/pt_helper
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: '8192'
        protocol: TCP
        targetPort: '8192'

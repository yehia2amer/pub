env:
  branch: master
image:
  pullPolicy: IfNotPresent
  repository: organizr/organizr
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

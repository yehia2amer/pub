env:
  File Lock Bypass: NoFileLock
image:
  pullPolicy: IfNotPresent
  repository: fjudith/pwm
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

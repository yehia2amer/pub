env:
  PWM_APPLICATIONFLAGS: NoFileLock
image:
  pullPolicy: IfNotPresent
  repository: fjudith/pwm
  tag: latest
persistence:
  appdata:
    enabled: true
    mountPath: /usr/share/pwm
service:
  main:
    ports:
      main:
        port: 9117
        targetPort: 9117
    protocol: HTTP

env:
  CHATID: ''
  DELAY: '60'
  TOKEN: ''
image:
  pullPolicy: IfNotPresent
  repository: bokker/rss.to.telegram
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

env: {}
image:
  pullPolicy: IfNotPresent
  repository: siwatinc/phlex
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        port: 8063
        protocol: TCP
        targetPort: 80
